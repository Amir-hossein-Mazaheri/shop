import React from "react";

import { BigHead } from "@bigheads/core";
import avatarOptions from "../Helpers/avatarOptions";

const RandomAvatar = () => {
  return <BigHead {...avatarOptions} />;
};

export default RandomAvatar;
